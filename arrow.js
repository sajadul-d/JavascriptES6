const add = (num1,num2) => num1 + num2;
const result = add(10,20);
console.log(result);


//bisal arrow function
const bisal = (x,y) => {
    const sum = x + y;
    const diff = x-y;
    const mul = x*y;
    return sum
}
const total = bisal(2,3);
console.log(total);