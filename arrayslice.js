const numbers = [1,2,3,4,5,6,7,8];
//slice is one kind of function which is slice a arry and it can not change the
//main array
const part = numbers.slice(2,5);
console.log(part);
//main array will not be changed
console.log(numbers);